export const formSelectors = {
  formSelector: '#contact-form',
  approvalSelector: '.form__cb',
  inputSelector: '.input-field__input',
  inputErrorClass: 'input-field__input_error',
  submitBtnSelector: '.form__submit',
};

export const spinnerSelectors = {
  id: '#spinner-form',
  textSelector: '.spinner__text',
  activeClass: 'spinner_active',
};

export const proceduresPopupSelector = '#popup-info';

export const procedureCardSelector = '.procedure-card';
export const procedureDescriptionSelector = '.procedure-card__description';

export const registrationAlertPopupSelector = '#popup-alert';

export const popupInfoSelectors = {
  infoBoxSelector: '.popup__info-box',
  resolvedAlertClass: 'popup__info-box_type_resolved',
  rejectedAlertClass: 'popup__info-box_type_rejected',
};
export const popupImgSelectors = {
  popupSelector: '#popup-img',
  imgElSelector: '.popup__img',
};

export const documentPhotoSelector = '.documents__photo';
export const registrationFormSelector = '#contact-form';
export const formInputSelector = '.input-field__input';
